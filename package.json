{
  "name": "app-store",
  "private": true,
  "scripts": {
    "dev:marketplace": "npm run dev --prefix apps/marketplace",
    "dev:sante": "npm run dev --prefix apps/sante"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "typescript": "^5"
  }
}
